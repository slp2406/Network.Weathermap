
# ------------------------------------------------------------------------------
# Panel Config
# ------------------------------------------------------------------------------

cellIdPreamble: ""

# ------------------------------------------------------------------------------
# Data (Prometheus queries)
# ------------------------------------------------------------------------------

data:
  DC1-LEAF-101-up:
    query: up{hostname="DC1-LEAF-101"}
    datasourceUid: prometheus

# ------------------------------------------------------------------------------
# Cells (map SVG ID → metric)
# ------------------------------------------------------------------------------

cells:

  elGet3u_EBpRw3bKElkN-146:
    dataRef: DC1-LEAF-101-up

    # Show value inside node (optional)
    label:
      mode: value

    # Fill color based on up/down
    fillColor:
      thresholds:
        - { color: "red", level: 0 }
        - { color: "green", level: 1 }

    # Optional: label color also changes
    labelColor:
      thresholds:
        - { color: "red", level: 0 }
        - { color: "green", level: 1 }
