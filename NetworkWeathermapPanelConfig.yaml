
# ------------------------------------------------------------------------------
# Panel Config
# ------------------------------------------------------------------------------

cellIdPreamble: ""

# ------------------------------------------------------------------------------
# Data section (Prometheus queries)
# ------------------------------------------------------------------------------

data:
  DC1-LEAF-101-up:
    query: up{hostname="DC1-LEAF-101"}
    datasourceUid: prometheus

# ------------------------------------------------------------------------------
# Cells (SVG mapping)
# ------------------------------------------------------------------------------

cells:

  cell-elGet3u_EBpRw3bKElkN-146:
    dataRef: DC1-LEAF-101-up

    # Display value inside the shape (optional)
    label:
      mode: value

    # Fill color based on up/down
    fillColor:
      thresholds:
        - { color: "red", level: 0 }
        - { color: "green", level: 1 }

    # Optional: change text color too
    labelColor:
      thresholds:
        - { color: "red", level: 0 }
        - { color: "green", level: 1 }
